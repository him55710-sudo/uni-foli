{
  "name": "uni-foli-monorepo",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "dev:infra": "docker-compose up -d",
    "dev:backend": "scripts\\start-api.cmd",
    "dev:frontend": "scripts\\start-frontend.cmd",
    "dev:all": "npm run dev:infra && concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "cd frontend && npm install && scripts\\setup-local.cmd sqlite"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
